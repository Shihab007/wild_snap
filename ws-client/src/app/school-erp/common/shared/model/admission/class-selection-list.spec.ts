import { ClassSelectionList } from './class-selection-list';

describe('ClassSelectionList', () => {
  it('should create an instance', () => {
    expect(new ClassSelectionList()).toBeTruthy();
  });
});
